import React from 'react';

const LoginPage = () => {
    return (
        <div>
            <h1>Login Page</h1>
            <p>Users can log in to their account or create a new account here.</p>
        </div>
    );
};

export default LoginPage;
